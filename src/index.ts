// Re-export everything for backward compatibility
export { extractJson, getJsonLeafPaths } from './utils/json'
export type { VirtualKeysDtsOptions, JSONValue, JSONObject, JSONArray } from './types'
export { default } from './plugin'